# SQLite（提出用）
DATABASE_URL=sqlite:///./app.db

# PostgreSQL（本番用）
# DATABASE_URL=postgresql+psycopg2://appuser:password@localhost:5432/expense

# Basic認証（提出用の固定値）
BASIC_AUTH_USER=admin
BASIC_AUTH_PASSWORD=secret123
